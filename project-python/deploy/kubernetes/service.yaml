apiVersion: v1
kind: Service
metadata:
  name: enterprise-ai-agent
  labels:
    app: enterprise-ai-agent
spec:
  type: ClusterIP
  selector:
    app: enterprise-ai-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
