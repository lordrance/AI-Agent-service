{
  "name": "project-python-dev",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "bash .cursor/scripts/cloud-install.sh",
  "start": "bash .cursor/scripts/cloud-start.sh",
  "terminals": [
    {
      "name": "fastapi",
      "command": "source project-python/.venv/bin/activate && cd project-python && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
      "description": "FastAPI 开发服务器（需 postgres/redis 已启动）"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "postgres", "port": 5432 },
    { "name": "redis", "port": 6379 }
  ]
}
